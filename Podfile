# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FireBasePostNotification' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FireBasePostNotification
  pod ‘Firebase’
  pod ‘Firebase/Messaging’
  pod 'Kingfisher', '~> 3.0'

  target 'FireBasePostNotificationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FireBasePostNotificationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
